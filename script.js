const loader=document.getElementById('loader');addEventListener('load',()=>setTimeout(()=>loader?.classList.add('done'),1700));setTimeout(()=>loader?.classList.add('done'),3000);
const header=document.querySelector('.site-header'),nav=document.querySelector('.site-header nav'),menu=document.querySelector('.menu-toggle'),progress=document.querySelector('.scroll-progress i');function updateScroll(){header?.classList.toggle('scrolled',scrollY>20);const max=Math.max(1,document.documentElement.scrollHeight-innerHeight);if(progress)progress.style.width=(scrollY/max*100)+'%'}addEventListener('scroll',updateScroll,{passive:true});updateScroll();menu?.addEventListener('click',()=>nav?.classList.toggle('open'));nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const manifestoButtons=[...document.querySelectorAll('[data-manifesto]')],manifestoArticles=[...document.querySelectorAll('.manifesto-content article')];function setManifesto(i){manifestoButtons.forEach((b,n)=>b.classList.toggle('active',n===i));manifestoArticles.forEach((a,n)=>a.classList.toggle('active',n===i))}manifestoButtons.forEach((b,i)=>b.addEventListener('click',()=>setManifesto(i)));
const serviceButtons=[...document.querySelectorAll('[data-service]')],serviceArticles=[...document.querySelectorAll('.service-view article')];function setService(i){serviceButtons.forEach((b,n)=>b.classList.toggle('active',n===i));serviceArticles.forEach((a,n)=>a.classList.toggle('active',n===i))}serviceButtons.forEach((b,i)=>b.addEventListener('click',()=>setService(i)));
const filterButtons=[...document.querySelectorAll('[data-filter]')],testimonialCards=[...document.querySelectorAll('.testimonial-track article')];filterButtons.forEach(btn=>btn.addEventListener('click',()=>{filterButtons.forEach(b=>b.classList.toggle('active',b===btn));const f=btn.dataset.filter;testimonialCards.forEach(card=>card.classList.toggle('hidden',f!=='all'&&card.dataset.cat!==f))}));
const counterObs=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,target=+el.dataset.count,start=performance.now(),dur=1000;const tick=t=>{const p=Math.min((t-start)/dur,1);el.textContent=Math.round(target*(1-Math.pow(1-p,3)));if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick);counterObs.unobserve(el)}),{threshold:.55});document.querySelectorAll('[data-count]').forEach(el=>counterObs.observe(el));
const revealObs=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;e.target.animate([{opacity:0,transform:'translateY(34px)'},{opacity:1,transform:'translateY(0)'}],{duration:720,easing:'cubic-bezier(.2,.78,.2,1)',fill:'both'});revealObs.unobserve(e.target)}),{threshold:.12});document.querySelectorAll('.section-head,.manifesto-stage,.metrics,.compare-wrap,.service-console,.result-story,.numbers,.filters,.testimonial-track,.contact-inner').forEach(el=>revealObs.observe(el));
const modal=document.getElementById('leadModal');function openModal(){modal?.classList.add('open');document.body.style.overflow='hidden'}function closeModal(){modal?.classList.remove('open');document.body.style.overflow=''}document.querySelectorAll('.js-open-modal').forEach(b=>b.addEventListener('click',openModal));modal?.querySelector('.modal-close')?.addEventListener('click',closeModal);modal?.querySelector('.modal-backdrop')?.addEventListener('click',closeModal);addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});document.getElementById('leadForm')?.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(e.currentTarget),msg=`Olá! Meu nome é ${fd.get('name')}. Meu negócio é ${fd.get('business')}. Meu WhatsApp é ${fd.get('phone')} e meu e-mail é ${fd.get('email')}. Quero conversar sobre crescimento da minha empresa.`;location.href='https://wa.me/5531996298572?text='+encodeURIComponent(msg)});
const dot=document.querySelector('.cursor-dot'),ring=document.querySelector('.cursor-ring'),system=document.querySelector('.hero-system');let mouseX=.5,mouseY=.5;addEventListener('mousemove',e=>{mouseX=e.clientX/innerWidth;mouseY=e.clientY/innerHeight;document.documentElement.style.setProperty('--mx',(mouseX*100)+'%');document.documentElement.style.setProperty('--my',(mouseY*100)+'%');if(system)system.style.transform=`rotateX(${(.5-mouseY)*5}deg) rotateY(${(mouseX-.5)*8}deg)`;if(dot){dot.style.left=e.clientX+'px';dot.style.top=e.clientY+'px'}if(ring)ring.animate({left:e.clientX+'px',top:e.clientY+'px'},{duration:160,fill:'forwards'})});
const canvas=document.getElementById('bridgeCanvas'),ctx=canvas?.getContext('2d');let w=0,h=0,dpr=1,travel=0,last=0,lastScroll=scrollY,boost=0;function resize(){if(!canvas||!ctx)return;dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0)}resize();addEventListener('resize',resize);addEventListener('scroll',()=>{const d=Math.abs(scrollY-lastScroll);lastScroll=scrollY;boost=Math.min(1.7,boost+d*.00025)},{passive:true});function line(x1,y1,x2,y2,a=.14,width=1,color='90,255,210'){ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.strokeStyle=`rgba(${color},${a})`;ctx.lineWidth=width;ctx.stroke()}function drawBridge(ts){if(!ctx)return;if(!last)last=ts;const dt=Math.min(36,ts-last);last=ts;travel=(travel+dt*(.00015+boost*.0012))%1;boost*=.95;ctx.clearRect(0,0,w,h);const vx=w/2+(mouseX-.5)*w*.075,vy=h*.34+(mouseY-.5)*h*.035,bottom=h*1.05,halfBottom=w*.34,halfTop=w*.018;const radial=ctx.createRadialGradient(vx,vy,0,vx,vy,h*.62);radial.addColorStop(0,'rgba(110,255,220,.12)');radial.addColorStop(.35,'rgba(30,190,130,.04)');radial.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=radial;ctx.fillRect(0,0,w,h);[-1,-.66,-.33,0,.33,.66,1].forEach((lane,i)=>line(vx+lane*halfTop,vy,vx+lane*halfBottom,bottom,i===3?.2:.085,i===3?1.4:.9,'115,255,220'));for(let i=0;i<30;i++){const p=((i/30)+travel)%1,z=Math.pow(p,2.35),y=vy+z*(bottom-vy),hw=halfTop+z*(halfBottom-halfTop),outer=hw*1.42,arch=(1-z)*h*.22+h*.018,a=.03+z*.2;line(vx-hw,y,vx+hw,y,a*.38,.9,'80,250,190');line(vx-outer,y,vx-hw,y-arch,a,1.15,'145,255,230');line(vx+outer,y,vx+hw,y-arch,a,1.15,'145,255,230');line(vx-hw,y-arch,vx+hw,y-arch,a*.9,1.05,'145,255,230')}ctx.fillStyle='rgba(0,6,4,.16)';ctx.fillRect(0,0,w,h);requestAnimationFrame(drawBridge)}requestAnimationFrame(drawBridge);